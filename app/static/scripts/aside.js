$(document).ready(function () {
	$('.edit_profile').click(function () {
		const message = `You can't fake your identity here...
		Well, at least for now.. But if you insiste. here is a trick.
		go to /finalize_profile and fill the form.
		`;

		alert(message);
	})
})
